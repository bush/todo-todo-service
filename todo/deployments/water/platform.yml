provider:
  environment:
    TODO_TABLE_NAME: ${self:service}-todo-table-${self:provider.stage}
    TODO_PK_NAME: pk
    TODO_SK_NAME: sk
  iam:
    role:
      statements:
        - ${file(./deployments/water/todo-table-permissions.yml)}
resources:
  - ${file(./deployments/water/todo-table.yml)}
